# Flicker Component
Flicker component for Unreal Engine with multiplayer support, RGB color cycling, and custom sequences.

## Installation
1. Create a `Plugins` folder in your Unreal Engine project directory
2. Copy the entire `HCSFlickerComponent` folder into `Plugins/`
3. Restart Unreal Engine
4. Enable the plugin in Edit → Plugins
